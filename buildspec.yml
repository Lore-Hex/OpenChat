version: 0.2

phases:
  pre_build:
    commands:
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 330422590279.dkr.ecr.us-east-1.amazonaws.com
      - IMAGE_URI=330422590279.dkr.ecr.us-east-1.amazonaws.com/openchat:latest
  build:
    commands:
      - docker build -t $IMAGE_URI .
  post_build:
    commands:
      - docker push $IMAGE_URI
